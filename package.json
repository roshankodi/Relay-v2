{
  "name": "relay",
  "version": "1.0.0",
  "private": true,
  "description": "Review media from a public Google Drive folder — zero-dependency Node.js build.",
  "type": "module",
  "engines": {
    "node": ">=18.17"
  },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --watch --env-file=.env server.js",
    "test": "node --test"
  },
  "dependencies": {},
  "devDependencies": {}
}
